# Server Configuration
PORT=5000

# Database Configuration (MySQL)
# Replace these values in your actual .env file with your local MySQL credentials
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Admin@123
DB_NAME=university_management_system

# Authentication
# Generate a strong random string for your actual .env file
JWT_SECRET=your_super_secret_jwt_key_here